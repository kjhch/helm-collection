# 无法使用HTTP
# app:
#   ingress:
#     enabled: true
#     ingressClassName: nginx
#     hosts: 
#       - hp-master.local
#     path: /k8s

# 开启HTTP自己创建ingress
kong:
  proxy:
    http:
      enabled: true


metrics-server:
  enabled: true